# Repository Instructions

本文件适用于整个仓库。更深层目录中的 `AGENTS.md` 可以补充局部规则，但不得降低本文件规定的提交前检查要求。

## 提交前强制流程

每次执行 `git commit` 前，Agent 必须按以下顺序完成全部步骤。任何一步未通过时，不得提交。

### 1. 代码审查

- 查看 `git status --short`、暂存区和工作区的完整 diff，确认提交范围准确且没有混入无关修改。
- 审查正确性、错误处理、兼容性、安全性、可维护性和潜在回归；发现问题后先修复，再重新审查。
- 运行与变更相匹配的测试、语法检查、静态检查或构建检查，并记录无法执行的检查及原因。
- 尊重用户已有改动，不覆盖、不回滚、不顺带提交与当前任务无关的内容。

### 2. 更新文件树

- 将仓库实际目录结构与根目录 `README.md` 中的文件树进行比较。
- 如果本次变更新增、删除、移动或重命名了文件树所展示的路径，必须同步更新 `README.md`。
- `README.md` 只保留文件树，不添加项目介绍、使用说明或其他正文。

### 3. 公开信息脱敏检查

- 检查准备提交的全部 diff 和新增文件，确认适合发布到公开仓库。
- 不得提交真实的 API Key、Access Token、密码、Cookie、私钥、证书密钥、Webhook、会话信息或其他凭据。
- 对个人邮箱、手机号、住址、身份证件、内部域名、内网 IP、主机名、绝对用户目录、群聊或用户标识等非公开信息进行删除、泛化或占位符替换。
- 示例配置只能使用明确的占位值，例如 `your-api-key`、`example.com`；不得把真实秘密伪装成示例。
- 若发现疑似敏感信息，不在日志、审查报告或提交信息中复述原值；只报告文件位置和信息类型，并在脱敏后重新检查。

## 提交门禁

完成上述三步后，再次检查最终 diff，并至少运行 `git diff --check`。只有在代码审查通过、README 文件树已同步、公开信息脱敏检查无异常时，Agent 才可以创建提交。
