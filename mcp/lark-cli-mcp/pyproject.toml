[project]
name = "lark-cli-mcp"
version = "0.1.0"
description = "OAuth-protected Streamable HTTP wrapper for lark-cli"
requires-python = ">=3.11"
dependencies = [
  "fastmcp==3.2.4",
  "py-key-value-aio[redis]==0.4.5",
]

[tool.uv]
package = false
