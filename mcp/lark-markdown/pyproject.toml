[project]
name = "lark-markdown"
version = "0.16.0"
description = "Authenticated FastMCP server for Markdown and Lark documents"
requires-python = ">=3.11"
dependencies = ["fastmcp==3.2.4"]

[tool.uv]
package = false
