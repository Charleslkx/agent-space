[project]
name = "firecrawl-mcp"
version = "0.1.0"
description = "OAuth-protected Streamable HTTP MCP wrapper for the firecrawl CLI"
requires-python = ">=3.11"
dependencies = [
  "fastmcp==3.2.4",
  "py-key-value-aio[redis]==0.4.5",
]

[tool.uv]
package = false
