# Claude Code Instructions

Claude 必须读取并遵守仓库根目录的 `AGENTS.md`，包括所有适用于当前文件的更深层 `AGENTS.md`。

每次执行 `git commit` 前，必须依次完成以下门禁：

1. 审查暂存区和工作区的完整变更，修复发现的问题，并运行适用的测试或检查。
2. 对照仓库结构更新根目录 `README.md` 文件树；README 只保留文件树。
3. 检查全部待提交内容是否适合公开，删除或替换凭据、个人信息、内部标识及其他敏感信息，且不得在输出中复述敏感原值。

任一步骤未完成或未通过时，不得提交。完成修复后必须重新执行对应检查，并在提交前运行 `git diff --check` 和最终 diff 审查。
